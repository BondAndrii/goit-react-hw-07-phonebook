import axios from "axios";

import contactsUrl from "../data/contactsLink.js";

axios.defaults.baseURL = contactsUrl;

async function fetchContacts() {
    const responce = await axios.get('/contacts');
    return responce.data;
}

export const api = {
    fetchContacts,
}